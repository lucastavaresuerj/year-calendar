type Title {
  name: String
  config: TitleConfig
}

type TitleConfig {
  aling: String
  size: String
  style: String
  fontWeight: String
  textColor: String
}

input InpTitle {
  name: String!
  config: InpTitleConfig
}

input InpTitleConfig {
  aling: String
  size: String
  style: String
  fontWeight: String
  textColor: String
}
