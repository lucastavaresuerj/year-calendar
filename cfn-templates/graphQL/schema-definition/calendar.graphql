type Calendar {
  name: String
  index: String
  user: String
  year: Int
  colNumber: Int
  title: Title
  defaultMonth: MonthGlobal
  months: [Month!]
}

extend type Query {
  calendar(id: Int): Calendar
}
